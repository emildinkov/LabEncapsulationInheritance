
namespace Players
{
    public class Elf : Hero // Наследяваме класа Hero и със control + точка генерира конструктор.
    {
        public Elf(string username, int level) : base(username, level)
        {
        }
    }
}
